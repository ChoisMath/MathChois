# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets (never commit real values)
.env
.env.*
!.env.example
!.env.*.example
!.env.sample

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
.playwright-mcp/

# Sensitive / local config
.claude.json
client_secret*.json
*.apps.googleusercontent.com.json
# backups (may contain secrets copied from .env)
*.bak
*.backup
# private keys / certs
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa
id_rsa.*
*service-account*.json
*serviceAccount*.json

# Artifacts
nul
image.png
*.tsbuildinfo

# Reference docs kept locally (curriculum PDFs etc.)
*.pdf

# Superpowers (brainstorm visual companion / local plan drafts / review scratch)
.superpowers/
.plans/
review.md

# Unused legacy code
legacy_rails/

# Local storage (dev file uploads)
local-storage/

# Drizzle (drizzle-kit generated output; schema.ts is source of truth, applied via db:push + startupMigrate)
drizzle/
